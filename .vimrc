" ============================================================================
" VIM CONFIGURATION (Root Directory)
" ============================================================================
" Sources shared settings for Vim and Neovim

" Load shared settings from the vim directory
source ~/.vim/settings.vim
